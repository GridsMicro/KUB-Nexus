[env:esp32dev]
platform = espressif32
board = esp32dev
framework = arduino
monitor_speed = 115200

lib_deps =
    bblanchon/ArduinoJson @ ^7.0.0
    ; Add project specific libraries here
    ; RNPacket, FailsafeManager, etc. (Assume custom local libs or add if available)

build_flags = 
    -D CORE_DEBUG_LEVEL=3
